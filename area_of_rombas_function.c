#include <stdio.h>
float areaofrombos();
int main()
{
    printf("%.2f", areaofrombos());
    return 0;
}
float areaofrombos()
{
    float p, q, area;
    printf("enter the p\nenter the p\n");
    scanf("%f%f", &p, &q);
    return area = (p * q) / 2;
}